{
  "name": "@tan-studio/desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun run build:service && tauri dev",
    "build": "bun run build:service && tauri build --bundles app && bun scripts/verify-test-app.ts",
    "bundle:installers": "bun run build:service && tauri build",
    "build:service": "bun scripts/build-service.ts",
    "test": "bun run build:service && cargo test --manifest-path src-tauri/Cargo.toml",
    "typecheck": "bun run build:service && cargo check --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tan-studio/web": "workspace:*",
    "@tauri-apps/cli": "^2.11.3"
  }
}
